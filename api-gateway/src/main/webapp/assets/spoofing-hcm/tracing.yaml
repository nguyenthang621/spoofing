fields:
  - fieldGroupClassName: row
    fieldGroup:
      - key: callingNumber
        type: tags
        className: col-md-4
        templateOptions:
          placeholder: Calling Number
          description: A list of prefix for matching under SMPP destination address
      - key: calledNumber
        type: tags
        className: col-md-4
        templateOptions:
          placeholder: Called Number
          description: A list of prefix for matching under SMPP source address
      - key: vlr
        type: tags
        className: col-md-4
        templateOptions:
          placeholder: VLR Number
          description: The VLR prefix to filter
