title:
  index: Whitelist
  view: View Whitelist ${ prefix }
  update: Update Whitelist ${ prefix }
  create: New Whitelist
  delete: Delete Whitelist ${ prefix }
apiEndpoint: /services/spoofing/api/whitelists
predicate: 'id'
reverse: true
columns:
  - prop: prefix
    label: Prefix
  - prop: length
    label: Length
  - prop: state
    label: State
    options:
      - value: 200
        label: Enable
      - value: 403
        label: Disable
  - description
fields:
  - fieldGroupClassName: row
    fieldGroup:
      - key: prefix
        type: input
        className: col-md-6
        templateOptions:
          label: MSISDN
          required: true
          description: Number Prefixes allow to skip Blacklist validation process
      - key: length
        type: input
        wrappers:
          - form-field
        className: col-md-6
        defaultValue: 0
        templateOptions:
          label: A Number Length
          type: number
          description: Source Address length to check against
          required: true
      - key: state
        type: select
        className: col-md-6
        defaultValue: 200
        templateOptions:
          label: State
          required: true
          options:
            - value: 200
              label: Enable
            - value: 403
              label: Disable
      - key: description
        type: textarea
        templateOptions:
          label: Description
          cols: 80
          rows: 5
